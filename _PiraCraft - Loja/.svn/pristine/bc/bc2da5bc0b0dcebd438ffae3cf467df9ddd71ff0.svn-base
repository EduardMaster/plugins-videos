package loja;

public class Server {

	private int port;

	public Server(int port) {
		this.port = port;
	}

	public int getPort() {
		return port;
	}
	/**
	 * public String getServer() { String retorno = null; try { ResultSet rs; rs
	 * = Main.getMysql().conectar().createStatement() .executeQuery("SELECT *
	 * FROM `MINIGAMES_SALAS_E_SERVIDORES` WHERE `PORTA`='" + Bukkit.getPort() +
	 * "';"); if (rs.next()) { retorno = rs.getString("NOMEDASALA"); }
	 * rs.getStatement().getConnection().close(); } catch (SQLException e) { }
	 * return retorno; }
	 **/
}
