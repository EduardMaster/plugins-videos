package me.douglasamv.hg.Comandos;

import org.bukkit.command.Command;
import org.bukkit.command.CommandExecutor;
import org.bukkit.command.CommandSender;
import org.bukkit.entity.Player;
import org.bukkit.event.Listener;

import me.douglasamv.hg.Main;

public class CMDStart implements Listener, CommandExecutor {

	@Override
	public boolean onCommand(CommandSender sender, Command cmd, String label, String[] args) {
		if (!(sender instanceof Player)) {
			sender.sendMessage("�cComando de Jogador!");
			return true;
		}
		Player p = (Player) sender;
		if (cmd.getName().equalsIgnoreCase("iniciar") && (p.hasPermission("hg.cmd.iniciar"))) {
			if (Main.PreGame) {
				Main.IniciarPartida();
			} else {
				p.sendMessage("§7[§a!§7] Você so pode usar esse comando no PreGame ou não possue permissão!");
			}
		}
		return false;
	}
}