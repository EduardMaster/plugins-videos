package net.eduard.economy.manager;

import org.bukkit.command.CommandSender;

public class MoneyTopGenerator {
	int id = 1;

	public MoneyTopGenerator(CommandSender sender) {

//		Main.getInstance().getManager().getCoins().entrySet().stream()
//				.sorted((e1, e2) -> e2.getValue().compareTo(e1.getValue())).limit(10).forEach(e -> {
//					sender.sendMessage("  §e " + id + "º §a" + e.getKey() + ": §2"
//							+ Main.getInstance().getFormater().format(e.getValue()));
//					id++;
//
//				});
	}

}
